# SimpleMarket Mobile

Flutter app — 5 tabs (首页/行情/自选/新闻/设置), WS live quotes,
offline snapshot cache (AC-009), 亚洲涨跌配色.

```bash
flutter pub get && flutter run
flutter test
API_URL=http://localhost:8000 flutter run --dart-define=API_URL=...
```
