# SimpleMarket Web

Next.js 14 + TS + Tailwind. i18n (zh/en/ja) + dark mode + 亚洲涨跌配色.

```bash
npm install && npm run dev        # http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:8000 npm run dev
npm run e2e                        # Playwright smoke
```
